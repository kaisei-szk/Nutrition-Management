# AGENTS.md

## 適用範囲

このファイルのルールはリポジトリ全体に適用する。

## リポジトリ境界

- このリポジトリは `Nutrition-Management` 単独で管理する。
- `SUZUKIKAISEI-personal` へ統合、コピー、移動しない。
- commitまたはpushの前に `git remote get-url origin` を実行し、接続先が `https://github.com/kaisei-szk/Nutrition-Management.git` であることを確認する。
- 別リポジトリのファイルや履歴を取り込まない。

## Privateリポジトリの安全ルール

- 本人の体重、食事履歴、運動履歴、食費は `data/records/` でGit管理する。
- 実データをcommitまたはpushする前に、GitHub上のリポジトリ可視性が `Private` であることを確認する。
- 生年月日、住所、メールアドレス、電話番号、正確な位置情報など、管理目的に不要な個人情報は保存しない。
- 認証情報、APIキー、アクセストークン、パスワード、Cookie、秘密鍵、`.env` を保存しない。
- 公開用テンプレートのCSVには、ヘッダーと明示的な架空サンプルだけを入れる。架空サンプルは `is_sample=true` とし、実在人物の記録と誤認されない未来の日付を使う。
- 初期構築コミットの `data/records/` はヘッダーだけとし、本人の実データを入れない。
- 実データから生成した週次・月次レポートは `reports/generated/` でGit管理する。
- コミット前に差分全体を読み、秘密情報や管理目的に不要な個人情報がないか確認する。
- リポジトリをPublicへ変更する場合は、実データを全Git履歴から除去し、漏えいがないことを確認してから変更する。

## 栄養・食費ルール

- 摂取カロリーの目安は1日1,800 kcalとする。
- タンパク質の目安は1日115〜125 gとする。
- 自炊予算は30日で20,000円とする。
- 外食費は自炊予算に含めない。
- 主食は米、パスタ、そうめん、そばを使う。
- オートミールは使用しない。
- 一人暮らし向けとし、作り置き・冷凍保存を重視する。

## レシピの必須項目

レシピを追加または更新するときは、次を必ず記載する。

- 材料と分量
- 合計金額
- 1食あたりの金額
- 1食あたりのカロリー
- 1食あたりのタンパク質
- 何食分か
- 冷蔵保存期間
- 冷凍保存期間

金額と栄養値が推定の場合は明記する。食品価格のサンプルは個人の購入履歴ではないことを明記する。

## 更新方法

1. 作業前に `README.md`、関連テンプレート、`git status`、`git remote -v` を確認する。
2. CSV列を変更するときは、同じ種類のサンプルとREADMEの説明を同時に更新する。
3. 設定値を変更するときは、README、設定テンプレート、週次・月次レポートの基準値を一致させる。
4. レシピは `recipes/TEMPLATE.md` を基準にし、必須項目を削除しない。
5. レポートは集計値の入力欄を保ち、自炊費と外食費を分ける。
6. 既存ファイルを削除する必要がある場合は、目的と影響を確認してから行う。
7. `git diff --check` と差分レビューを行い、秘密情報や不要な個人情報がないことを確認する。
8. commitとpushは、このリポジトリのみに行う。

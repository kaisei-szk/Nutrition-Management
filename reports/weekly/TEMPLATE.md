# 週次レポート: `<YYYY-MM-DD>` 〜 `<YYYY-MM-DD>`

> GitHub上の可視性がPrivateであることを確認し、完成版を `reports/generated/` に保存してGit管理する。

## サマリー

| 指標 | 今週 | 目安 | 判定・メモ |
| --- | ---: | ---: | --- |
| 1日平均摂取カロリー | `<値>` kcal | 1,800 kcal | `<メモ>` |
| 1日平均タンパク質 | `<値>` g | 115〜125 g | `<メモ>` |
| 週初めの体重 | `<値>` kg | — | `<メモ>` |
| 週末の体重 | `<値>` kg | — | `<メモ>` |
| 運動回数 | `<値>` 回 | — | `<メモ>` |
| 運動時間 | `<値>` 分 | — | `<メモ>` |
| 自炊費 | `<値>` 円 | 30日20,000円 | `<メモ>` |
| 外食費（予算外） | `<値>` 円 | 自炊予算に含めない | `<メモ>` |

## 日別集計

| 日付 | カロリー | タンパク質 | 体重 | 運動時間 | 自炊費 |
| --- | ---: | ---: | ---: | ---: | ---: |
| `<YYYY-MM-DD>` | `<値>` kcal | `<値>` g | `<値>` kg | `<値>` 分 | `<値>` 円 |

## 振り返り

- うまくいったこと: `<内容>`
- 改善すること: `<内容>`
- 作り置き・冷凍の状況: `<内容>`
- 次週の方針: `<内容>`
